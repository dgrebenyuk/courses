Есть четыре вида скобок '{' '[' '(' '<'
Каждой из них соответствуют закрывающиеся скобки '}' ']' ')' '>'

Расширить класс String методом valid? для проверки строки состоящий только из скобок на валидность

# "{(})".valid? => false
# "{{[]".valid? => false
# "{[()]}".valid? => true
